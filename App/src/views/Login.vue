<script setup>
// Placeholder scaffold view. Replaced by the ported Login.vue in PR 6
// (App — Login + Home Port, mobile-capacitor-setup Phase 6). Exists now so
// the 401 response interceptor's redirect target (see services/api.js)
// resolves to a real route instead of an unmatched path.
</script>

<template>
  <main class="flex min-h-screen items-center justify-center">
    <p>Redirecting to login…</p>
  </main>
</template>
